// Generated by CoffeeScript 1.7.1
(function() {
  jQuery(function() {
    var clock, entry, i, now, offset, startButton, stopButton, timer, _i, _startTimer, _stopTimer;
    entry = new Entry;
    timer = $('.timer');
    startButton = $('.start-button');
    stopButton = $('.stop-button');
    now = null;
    offset = null;
    clock = null;
    i = 0;
    console.log(entry.properties.entryDate);
    startButton.click(function() {
      entry.setStartTime();
      _startTimer();
      return console.log(entry.properties.startTime);
    });
    stopButton.click(function() {
      entry.setEndTime();
      _stopTimer();
      return console.log(entry.properties.endTime);
    });
    _startTimer = function() {
      return console.log("clicked start");
    };
    _stopTimer = function() {
      return console.log("clicked stop");
    };
    return _i = function() {
      i++;
      return timer.html(i);
    };
  });


  /*
  setInverval(function to update the dom with new time text, 1000)
  
  function to update the dom
  inside has to do something like this
    i = Date.now
    newTime = i++
    timer.html(newTime)
  
  need setInverval set to a variable so I can call clearInterval on it when stop is clicked
  
  - set start (now)
  - set offset (new now)
  - time is offset - start
  - update offset every second
  - compute
   */

}).call(this);

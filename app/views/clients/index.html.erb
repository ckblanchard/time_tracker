<div class="page-header">
  <h1>Client list<br/>
  <small>All your invoicable clients.</small></h1>
  
  <%= link_to "Add a new client", new_client_path, class: "btn btn-primary" %>
</div>

<table class="table table-striped table-hover">
  <thead>
    <th>Company</th>
    <th>Contact</th>
    <th>Address</th>
    <th>Phone number</th>
    <th></th>
  </thead>
  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= client.company %></td>
      </tr>
      <tr>
        <td><%= client.contact_name %></td>
      </tr>
      <tr>
        <td>
          <address>
            <%= client.address %><br>
            <%= client.address2 %><br>
            <%= client.city %>, <%= client.state %> <%= client.zip_code %><br>
          </address>  
        </td>
      </tr>
      <tr>
        <td><%= client.phone_number %></td>
      </tr>
      <tr>
        <td><%= link_to "Edit", edit_client_path(client), class: "btn, btn-sm, btn-primary" %></td>
      </tr>
      <% end %>
  </tbody>
</table>
